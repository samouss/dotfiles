[user]
	name = Vaillant Samuel
	email = samuel.vllnt@gmail.com

[color]
  ui = true

[alias]
	co = checkout
	sw = switch
	st = status --short
	stl = status
	br = branch -vv
	undo = reset --soft HEAD^
	wip = !git add -A && git commit -m 'WIP'

[push]
	default = simple
	followTags = true

[merge]
	ff = only
